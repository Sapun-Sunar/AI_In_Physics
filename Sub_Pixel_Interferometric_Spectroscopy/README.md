# Sub-Pixel Interferometric Spectroscopy

**Architect:** Sapun Sunar  
**Domain:** Computational Physics, Quantum Optics, Bare-Metal Neural Architecture  

## Overview
This repository contains a fully custom, bare-metal Convolutional Neural Network (CNN) built from the ground up in PyTorch. It is designed to perform sub-pixel telemetry on quantum absorption features (Lorentzian FWHM) within a simulated Fabry-Perot interference cavity. 

By modeling the exact thermodynamic and optical mechanics of a cold gas chamber, this architecture bypasses standard Nyquist optical sampling limits, accurately resolving localized absorption dips to an accuracy of ~0.2 physical pixels against a heavy Poisson and Gaussian noise floor.

## The Physics Engine (The Live Feeder)
Instead of relying on static datasets, this pipeline utilizes a Live Feeder Machine that continuously forges mathematically unique universes on the CPU while the GPU/MPS trains.

* **Continuum Generation (`continuum.py`):** Spawns a 250x400 landscape featuring a true-to-life 2D Gaussian thermal plasma envelope.
* **Wave Mechanics & Absorption (`interference.py`):** Projects an Airy transmission mask ($m \lambda$ Fabry-Perot rings) over the continuum. It autonomously identifies the Zero-Order cavity resonance ($m=0$, the central bullseye) and carves expanding Lorentzian absorption craters across the spatial plane based on a randomized true FWHM target.
* **Sensor Degradation (`noise.py`):** Applies strict Quantum Shot Noise (Poisson) and Sensor Read Noise (Gaussian thermal variance) to the pristine optics, perfectly mimicking physical laboratory hardware limits.

## The Neural Architecture (`brain.py`)
The `SpectroscopicBrain` is not a generic classifier; it is a custom topological extractor engineered for high-voltage stability and sub-pixel resolution.

* **Contrast-Inverting Lenses & Peak Preservation:** Utilizes greedy `MaxPool2d` layers to intentionally preserve unadulterated high-voltage spikes generated by autonomously evolved, contrast-inverting convolutional kernels, saving sub-Nyquist features from being smoothed into the noise floor.
* **Covariate Impedance Matching:** To survive infinite-plateau training loops without suffering from Covariate Initialization Shock, an `nn.BatchNorm1d` surge protector is soldered directly to the 198,400-wire `Flatten` boundary. This stabilizes the raw electrical variance before it strikes the dense metropolis.
* **The Telemetric Normalizer:** Employs a mathematically isolated `FWHM_NORMALIZER` to map physical widths ($3.0\mu\text{m}$ to $7.0\mu\text{m}$) strictly into the $[0.3, 0.7]$ boundary. This traps the network's final predictions in the most highly elastic, gradient-rich valley of the Sigmoid activation curve.

## The Multi-Stage Gatekeeper (`train.py`)
Standard early stopping destroys models before they can compound their learnings. This repository utilizes a custom Multi-Stage Transmission Gatekeeper. Instead of triggering a hard termination when validation loss plateaus, the Gatekeeper automatically drops the learning rate by a factor of 10, executing surgical fine-tuning across multiple transmission gears until absolute bare-metal convergence is achieved. 

**Current Validation Telemetry:** MSE ~ `0.0004` (Equivalent to ~0.2 pixel physical accuracy).

## Future Directives & Development
> **[!] STATUS:** *Active Campaign. This architecture is still under development towards perfection.*

While the foundational zero-entropy manifold is mathematically indestructible, the instrument is actively being upgraded for true high-energy deployments. Future updates will include:
1. **Asymmetrical Topological Chaos:** Reactivating the `slant_gradient` to introduce intense spatial asymmetries into the plasma continuum, as well as physical hardware degradations.
2. **C++ / ROOT Migration:** Porting the heavy physical dataset generators from NumPy to C++ and CERN's ROOT framework to achieve the computational velocity required for massive particle collision scaling.